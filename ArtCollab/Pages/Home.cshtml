@page
@model ArtCollab.Pages.HomeModel
@{
    ViewData["Title"] = "Home";
}

@section Styles {
    <style>
        .filter-btn {
            font-size: 1.2rem;
            padding: 12px 24px;
            min-width: 120px;
            margin: 6px;
            border-radius: 0.25rem;
        }

            .filter-btn.active {
                background-color: black;
                color: white;
            }

            .filter-btn:hover {
                background-color: #f0f0f0;
            }
    </style>
}

<div class="container-fluid landing-page">

    <!-- Filter Knoppen -->
    <div class="filter-bar text-center mt-4">
        <button class="filter-btn active">All</button>
        <button class="filter-btn">2023</button>
        <button class="filter-btn">2022</button>
        <button class="filter-btn">2021</button>
        <!-- Voeg meer jaren toe -->
    </div>

    <!-- Artwork Grid -->
    <div class="container mt-4">
        <div class="row row-cols-2 row-cols-sm-3 row-cols-md-4 g-3">
            @foreach (var artwork in Model.Artworks)
            {
                <div class="col">
                    <div class="card h-100 shadow-sm">
                        <img src="@artwork.ImageUrl" class="card-img-top" alt="@artwork.Title" />
                        <div class="card-body">
                            <h6 class="card-title text-truncate">@artwork.Title</h6>
                        </div>
                    </div>
                </div>
            }
        </div>

        <div class="text-center mt-4">
            <a href="#" class="btn btn-outline-dark">Next ></a>
        </div>
    </div>
</div>
