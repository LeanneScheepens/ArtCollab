@using System.Security.Claims
@{
    var role = Context.User?.FindFirst(ClaimTypes.Role)?.Value;

    Layout = role switch
    {
        "Admin" => "_AdminLayout",
        "Artist" => "_ArtistLayout",
        _ => "_Layout"
    };
}
